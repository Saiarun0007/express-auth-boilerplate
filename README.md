# express-auth-boilerplate
A robust Node.js authentication system built with Express, MongoDB, and EJS. Features secure password hashing with Bcrypt, session-based state management, and protected routing middleware to ensure private data remains accessible only to authenticated users.

/verySecret
